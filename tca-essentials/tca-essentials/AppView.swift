import ComposableArchitecture
import SwiftUI

struct AppView: View {
    let store1: StoreOf<CounterReducer>
    let store2: StoreOf<CounterReducer>

    var body: some View {
        TabView {
            
        }
    }
}
